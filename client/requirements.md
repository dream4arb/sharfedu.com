## Packages
framer-motion | For smooth animations and transitions
lucide-react | For beautiful icons (already in base stack, but confirming usage)
clsx | For conditional class names (already in base stack)
tailwind-merge | For merging tailwind classes (already in base stack)

## Notes
Tailwind Config: Extend font family to include 'Cairo' for Arabic support.
Direction: All pages must be wrapped in a layout that enforces `dir="rtl"`.
Auth: Login button should link to `/api/login` (Replit Auth).
Images: Use Unsplash for hero/static images with educational themes.
